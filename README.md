# zotero-bridge
This script is designed to be read-only and local. It targets a specific Zotero Collection by name (identifier) and outputs a JSON object that includes the metadata and absolute file paths for the PDFs.
